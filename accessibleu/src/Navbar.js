const Navbar = () => {
    return (
        <nav className="NavBar">
            <div className="links">
                <a href="/">Home</a>
                <a href="/Login">Login</a>
            </div>
        </nav>
    );
}

export default Navbar;